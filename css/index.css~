body {
    font-family: serif;
    margin: 70px;
    font-size: 14pt;
    line-height: 1.5;
}

.site {
    font-size: 30px;
    color: #777;
    text-align: left;
}
.sitename {
	font-family: Times New Roman;
	font-size: 18pt;
	font-weight: bold;
}
#copyright {
    text-align: right;
    font-family: Times New Roman;
    font-size: 14pt;
    font-style: italic;
    font-weight: bold;
    color: #222;
}
#copyright a {color:#222;}

a { text-decoration: none; color: #556677; }
a:visited { text-decoration: none; }  
a:hover { text-decoration:underline; color: black;}

#content a { font-size: 20px; font-weight: bold;}
#content ul {list-style:none;}
.connect h2 { margin-top: 50px; color: #777;}
.connect a{ color: #333; }
.connect ul {list-style: circle;}

.posts {
    font-family: Monaco, "Courier New", monospace;	
	font-size: 10pt;
	list-style-type: none;
	margin-bottom: 2em;		
}
ul.posts span {
    color: #aaa;
    font-family: Monaco, "Courier New", monospace;
    font-size: 10pt;
  }

